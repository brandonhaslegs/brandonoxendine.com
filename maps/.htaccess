RewriteEngine On
RewriteBase /maps/

# Keep direct requests for existing files/directories.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# SPA fallback for pretty routes.
RewriteRule . /maps/index.html [L]
